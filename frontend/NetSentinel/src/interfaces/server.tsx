export interface Server {
  id: number;
  ip: string;
  name: string;
  location: string;
  assigned_id: string;
  is_active: number;
  last_checks: string;
  last_check_at: string;
  status: string;
}
